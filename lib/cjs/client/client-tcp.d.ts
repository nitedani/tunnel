export declare const listen: ({ PROVIDER, TO_PROTOCOL, TO_PORT, TO_HOST, SECURE, PREFERRED_PORT, }: {
    PROVIDER: any;
    TO_PROTOCOL: any;
    TO_PORT: any;
    TO_HOST: any;
    SECURE: any;
    PREFERRED_PORT: any;
}) => Promise<void>;
